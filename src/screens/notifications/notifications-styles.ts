import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    flex: 1
  },
  text_bold: {
    color: 'black',
    fontWeight: '600',
    padding: 10
  }
})

export default styles