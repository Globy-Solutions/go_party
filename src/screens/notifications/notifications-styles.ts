import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center'
  },
  text_bold: {
    fontWeight: '600',
    color: 'black',
    padding: 10
  }
})

export default styles